# Copy to .env and fill in values
# The CLI loads .env automatically from the current working directory

# OpenAI (required for LLM analysis)
OPENAI_API_KEY=sk-...

# Optional: override default model
OPENAI_MODEL=gpt-4o-mini

# Optional: GitHub token override (otherwise uses `gh auth login`)
# GITHUB_TOKEN=ghp_...
